[project]
name = "pku-poster-layout"
version = "0.1.0"
description = "PKU PosterLayout dataset loader for Hugging Face Datasets"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets[vision]>=2.0.0,<4.0.0",
    "pandas>=2.0.0",
]
