[project]
name = "lica"
version = "0.1.0"
description = "Hugging Face dataset loader for LICA graphic design layout compositions."
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "datasets[vision]>=2.0.0,<4.0.0",
]
