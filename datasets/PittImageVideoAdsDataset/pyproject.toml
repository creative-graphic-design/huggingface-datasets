[project]
name = "pitt-image-video-ads-dataset"
version = "0.1.0"
description = "Pitt image and video advertisement annotations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["datasets[vision]>=2.0.0,<4.0.0"]
