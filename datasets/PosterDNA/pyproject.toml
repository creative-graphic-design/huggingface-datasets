[project]
name = "poster-dna"
version = "0.1.0"
description = "PosterDNA poster images, HTML layout specifications, and intention metadata from PosterVerse."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets[vision]>=2.0.0,<4.0.0",
]
