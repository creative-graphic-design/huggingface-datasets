[project]
name = "creative-psd"
version = "0.1.0"
description = "Hugging Face dataset loader for CreativePSD PSD-derived graphic design archives."
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "datasets[vision]>=2.0.0,<4.0.0",
    "modelscope>=1.0.0",
]
